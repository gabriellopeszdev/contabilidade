# Two-Factor Authentication (2FA) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar TOTP 2FA (Google Authenticator / Authy) ao login de contadores e clientes, com códigos de backup e UI de configuração nas preferências.

**Architecture:** Login retorna `{ requires2FA: true, tempToken }` quando 2FA está ativo. O tempToken (JWT curto, 5min) é trocado pelo token definitivo após verificação do TOTP. O segredo TOTP é armazenado criptografado com AES-256 usando `JWT_SECRET` como chave derivada. Backup codes são armazenados como hashes bcrypt.

**Tech Stack:** `otplib` (TOTP), `qrcode` (já instalado), `crypto` (Node built-in para AES), `bcryptjs` (já instalado)

---

## Arquivo Map

| Ação | Arquivo |
|------|---------|
| Modify | `prisma/schema.prisma` |
| Create | `prisma/migrations/YYYYMMDD_2fa/migration.sql` (via `npx prisma migrate dev`) |
| Create | `src/lib/totp.ts` |
| Modify | `app/api/v1/auth/login/route.ts` |
| Create | `app/api/v1/auth/2fa/setup/route.ts` |
| Create | `app/api/v1/auth/2fa/enable/route.ts` |
| Create | `app/api/v1/auth/2fa/disable/route.ts` |
| Create | `app/api/v1/auth/2fa/verify/route.ts` |
| Create | `app/api/v1/auth/2fa/backup/route.ts` |
| Modify | `app/(contador)/configuracoes/page.tsx` (seção 2FA) |
| Modify | `app/(auth)/page.tsx` (step 2FA no login) |

---

## Task 1: Schema — adicionar campos 2FA

**Files:**
- Modify: `prisma/schema.prisma`

- [ ] **Step 1: Adicionar campos ao UsuarioContador e UsuarioCliente**

Em `prisma/schema.prisma`, adicionar dentro de `model UsuarioContador` (após `resetTokenExpiresAt`):

```prisma
  twoFactorEnabled     Boolean   @default(false) @map("two_factor_enabled")
  twoFactorSecret      String?   @map("two_factor_secret") @db.VarChar(500)
  twoFactorBackupCodes String[]  @map("two_factor_backup_codes")
```

E dentro de `model UsuarioCliente` (após `activatedAt`):

```prisma
  twoFactorEnabled     Boolean   @default(false) @map("two_factor_enabled")
  twoFactorSecret      String?   @map("two_factor_secret") @db.VarChar(500)
  twoFactorBackupCodes String[]  @map("two_factor_backup_codes")
```

- [ ] **Step 2: Gerar e aplicar migration**

```bash
npx prisma migrate dev --name add_2fa_fields
```

Esperado: migration criada e aplicada com sucesso.

- [ ] **Step 3: Commit**

```bash
git add prisma/
git commit -m "feat(2fa): add 2FA fields to user tables"
```

---

## Task 2: Biblioteca TOTP

**Files:**
- Create: `src/lib/totp.ts`

- [ ] **Step 1: Instalar otplib**

```bash
npm install otplib
```

- [ ] **Step 2: Criar src/lib/totp.ts**

```typescript
import { authenticator } from 'otplib';
import { createCipheriv, createDecipheriv, randomBytes, scryptSync } from 'crypto';
import bcrypt from 'bcryptjs';

const ALGORITHM = 'aes-256-gcm';

function getDerivedKey(): Buffer {
  const secret = process.env.JWT_SECRET;
  if (!secret) throw new Error('JWT_SECRET não configurado');
  return scryptSync(secret, 'totp-salt-v1', 32);
}

export function encryptSecret(plaintext: string): string {
  const key = getDerivedKey();
  const iv = randomBytes(12);
  const cipher = createCipheriv(ALGORITHM, key, iv);
  const encrypted = Buffer.concat([cipher.update(plaintext, 'utf8'), cipher.final()]);
  const tag = cipher.getAuthTag();
  return [iv.toString('hex'), tag.toString('hex'), encrypted.toString('hex')].join(':');
}

export function decryptSecret(ciphertext: string): string {
  const [ivHex, tagHex, encHex] = ciphertext.split(':');
  const key = getDerivedKey();
  const decipher = createDecipheriv(ALGORITHM, key, Buffer.from(ivHex, 'hex'));
  decipher.setAuthTag(Buffer.from(tagHex, 'hex'));
  return decipher.update(Buffer.from(encHex, 'hex')).toString('utf8') + decipher.final('utf8');
}

export function generateSecret(): string {
  return authenticator.generateSecret(20);
}

export function generateOtpAuthUrl(email: string, secret: string): string {
  return authenticator.keyuri(email, 'Konto Contábil', secret);
}

export function verifyToken(token: string, encryptedSecret: string): boolean {
  try {
    const secret = decryptSecret(encryptedSecret);
    return authenticator.verify({ token, secret });
  } catch {
    return false;
  }
}

export async function generateBackupCodes(): Promise<{ plain: string[]; hashed: string[] }> {
  const plain = Array.from({ length: 8 }, () =>
    randomBytes(4).toString('hex').toUpperCase()
  );
  const hashed = await Promise.all(plain.map((c) => bcrypt.hash(c, 10)));
  return { plain, hashed };
}

export async function verifyBackupCode(
  code: string,
  hashedCodes: string[]
): Promise<number> {
  for (let i = 0; i < hashedCodes.length; i++) {
    const match = await bcrypt.compare(code, hashedCodes[i]);
    if (match) return i;
  }
  return -1;
}
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/totp.ts
git commit -m "feat(2fa): add TOTP utility (encrypt/decrypt secret, verify token, backup codes)"
```

---

## Task 3: Modificar login para suportar 2FA

**Files:**
- Modify: `app/api/v1/auth/login/route.ts`

- [ ] **Step 1: Ler o arquivo atual**

Leia `app/api/v1/auth/login/route.ts` para entender o fluxo atual.

- [ ] **Step 2: Adicionar lógica 2FA ao login**

Após a verificação de senha bem-sucedida, antes de gerar o JWT final, checar se 2FA está ativo:

```typescript
// Após verificar senha com bcrypt...
if (usuario.twoFactorEnabled && usuario.twoFactorSecret) {
  // Gerar tempToken com 5 min de validade
  const tempToken = await new SignJWT({
    sub: usuario.id,
    type: 'TEMP_2FA',
    role: tipo === 'contador' ? 'ACCOUNTANT' : 'CLIENT',
  })
    .setProtectedHeader({ alg: 'HS256' })
    .setExpirationTime('5m')
    .sign(new TextEncoder().encode(process.env.JWT_SECRET!));

  return NextResponse.json({ requires2FA: true, tempToken }, { status: 200 });
}
// Se 2FA não ativo, continua gerando JWT normal...
```

- [ ] **Step 3: Commit**

```bash
git add app/api/v1/auth/login/route.ts
git commit -m "feat(2fa): return tempToken when 2FA is enabled on login"
```

---

## Task 4: Rota de setup 2FA

**Files:**
- Create: `app/api/v1/auth/2fa/setup/route.ts`

- [ ] **Step 1: Criar rota**

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { withAuth } from '@/src/infrastructure/http/middlewares/withAuth';
import { prisma } from '@/src/infrastructure/database/prisma';
import { generateSecret, generateOtpAuthUrl, encryptSecret } from '@/src/lib/totp';
import QRCode from 'qrcode';

export const GET = withAuth(async (req, ctx) => {
  const userId = ctx.auth.sub;
  const role = ctx.auth.role;

  // Buscar email do usuário
  const usuario = role === 'ACCOUNTANT'
    ? await prisma.usuarioContador.findUnique({ where: { id: userId }, select: { email: true, twoFactorEnabled: true } })
    : await prisma.usuarioCliente.findUnique({ where: { id: userId }, select: { email: true, twoFactorEnabled: true } });

  if (!usuario) return NextResponse.json({ message: 'Usuário não encontrado' }, { status: 404 });
  if (usuario.twoFactorEnabled) return NextResponse.json({ message: '2FA já está ativado' }, { status: 400 });

  const secret = generateSecret();
  const otpAuthUrl = generateOtpAuthUrl(usuario.email, secret);
  const qrCodeDataUrl = await QRCode.toDataURL(otpAuthUrl);
  const encryptedSecret = encryptSecret(secret);

  // Salvar segredo temporário (ainda não ativado — ativação só após verificação)
  if (role === 'ACCOUNTANT') {
    await prisma.usuarioContador.update({ where: { id: userId }, data: { twoFactorSecret: encryptedSecret } });
  } else {
    await prisma.usuarioCliente.update({ where: { id: userId }, data: { twoFactorSecret: encryptedSecret } });
  }

  return NextResponse.json({ qrCode: qrCodeDataUrl, secret });
}, ['ACCOUNTANT', 'CLIENT', 'EMPLOYEE']);
```

- [ ] **Step 2: Commit**

```bash
git add app/api/v1/auth/2fa/setup/route.ts
git commit -m "feat(2fa): add 2FA setup endpoint (generates QR code)"
```

---

## Task 5: Rotas enable / disable / verify / backup

**Files:**
- Create: `app/api/v1/auth/2fa/enable/route.ts`
- Create: `app/api/v1/auth/2fa/disable/route.ts`
- Create: `app/api/v1/auth/2fa/verify/route.ts`
- Create: `app/api/v1/auth/2fa/backup/route.ts`

- [ ] **Step 1: enable/route.ts** — confirma TOTP e ativa 2FA + gera backup codes

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { withAuth } from '@/src/infrastructure/http/middlewares/withAuth';
import { prisma } from '@/src/infrastructure/database/prisma';
import { verifyToken, generateBackupCodes } from '@/src/lib/totp';

export const POST = withAuth(async (req, ctx) => {
  const { token } = await req.json();
  if (!token || typeof token !== 'string') {
    return NextResponse.json({ message: 'Token obrigatório' }, { status: 400 });
  }

  const userId = ctx.auth.sub;
  const isContador = ctx.auth.role === 'ACCOUNTANT';

  const usuario = isContador
    ? await prisma.usuarioContador.findUnique({ where: { id: userId }, select: { twoFactorSecret: true, twoFactorEnabled: true } })
    : await prisma.usuarioCliente.findUnique({ where: { id: userId }, select: { twoFactorSecret: true, twoFactorEnabled: true } });

  if (!usuario?.twoFactorSecret) {
    return NextResponse.json({ message: 'Execute o setup primeiro' }, { status: 400 });
  }
  if (usuario.twoFactorEnabled) {
    return NextResponse.json({ message: '2FA já está ativo' }, { status: 400 });
  }

  const valid = verifyToken(token, usuario.twoFactorSecret);
  if (!valid) return NextResponse.json({ message: 'Token inválido' }, { status: 400 });

  const { plain, hashed } = await generateBackupCodes();

  if (isContador) {
    await prisma.usuarioContador.update({
      where: { id: userId },
      data: { twoFactorEnabled: true, twoFactorBackupCodes: hashed },
    });
  } else {
    await prisma.usuarioCliente.update({
      where: { id: userId },
      data: { twoFactorEnabled: true, twoFactorBackupCodes: hashed },
    });
  }

  return NextResponse.json({ backupCodes: plain });
}, ['ACCOUNTANT', 'CLIENT', 'EMPLOYEE']);
```

- [ ] **Step 2: disable/route.ts** — desativa 2FA após confirmação de senha

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { withAuth } from '@/src/infrastructure/http/middlewares/withAuth';
import { prisma } from '@/src/infrastructure/database/prisma';
import bcrypt from 'bcryptjs';

export const POST = withAuth(async (req, ctx) => {
  const { senha } = await req.json();
  if (!senha) return NextResponse.json({ message: 'Senha obrigatória' }, { status: 400 });

  const userId = ctx.auth.sub;
  const isContador = ctx.auth.role === 'ACCOUNTANT';

  const usuario = isContador
    ? await prisma.usuarioContador.findUnique({ where: { id: userId }, select: { passwordHash: true } })
    : await prisma.usuarioCliente.findUnique({ where: { id: userId }, select: { passwordHash: true } });

  if (!usuario?.passwordHash) return NextResponse.json({ message: 'Usuário não encontrado' }, { status: 404 });

  const ok = await bcrypt.compare(senha, usuario.passwordHash);
  if (!ok) return NextResponse.json({ message: 'Senha incorreta' }, { status: 401 });

  if (isContador) {
    await prisma.usuarioContador.update({
      where: { id: userId },
      data: { twoFactorEnabled: false, twoFactorSecret: null, twoFactorBackupCodes: [] },
    });
  } else {
    await prisma.usuarioCliente.update({
      where: { id: userId },
      data: { twoFactorEnabled: false, twoFactorSecret: null, twoFactorBackupCodes: [] },
    });
  }

  return NextResponse.json({ message: '2FA desativado com sucesso' });
}, ['ACCOUNTANT', 'CLIENT', 'EMPLOYEE']);
```

- [ ] **Step 3: verify/route.ts** — troca tempToken + TOTP pelo JWT definitivo

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { jwtVerify, SignJWT } from 'jose';
import { prisma } from '@/src/infrastructure/database/prisma';
import { verifyToken } from '@/src/lib/totp';

export async function POST(req: NextRequest) {
  const { tempToken, totpToken } = await req.json();

  if (!tempToken || !totpToken) {
    return NextResponse.json({ message: 'tempToken e totpToken obrigatórios' }, { status: 400 });
  }

  const key = new TextEncoder().encode(process.env.JWT_SECRET!);

  let payload: { sub: string; type: string; role: string };
  try {
    const result = await jwtVerify(tempToken, key);
    payload = result.payload as typeof payload;
  } catch {
    return NextResponse.json({ message: 'Token temporário inválido ou expirado' }, { status: 401 });
  }

  if (payload.type !== 'TEMP_2FA') {
    return NextResponse.json({ message: 'Token inválido' }, { status: 401 });
  }

  const isContador = payload.role === 'ACCOUNTANT';
  const usuario = isContador
    ? await prisma.usuarioContador.findUnique({ where: { id: payload.sub }, select: { twoFactorSecret: true, isActive: true } })
    : await prisma.usuarioCliente.findUnique({ where: { id: payload.sub }, select: { twoFactorSecret: true, isActive: true } });

  if (!usuario?.isActive || !usuario.twoFactorSecret) {
    return NextResponse.json({ message: 'Usuário inativo ou 2FA não configurado' }, { status: 401 });
  }

  const valid = verifyToken(totpToken, usuario.twoFactorSecret);
  if (!valid) return NextResponse.json({ message: 'Código 2FA inválido' }, { status: 401 });

  const expiresIn = process.env.JWT_EXPIRES_IN ?? '8h';
  const token = await new SignJWT({ sub: payload.sub, role: payload.role })
    .setProtectedHeader({ alg: 'HS256' })
    .setExpirationTime(expiresIn)
    .sign(key);

  return NextResponse.json({ token });
}
```

- [ ] **Step 4: backup/route.ts** — usa código de backup para autenticar

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { jwtVerify, SignJWT } from 'jose';
import { prisma } from '@/src/infrastructure/database/prisma';
import { verifyBackupCode } from '@/src/lib/totp';

export async function POST(req: NextRequest) {
  const { tempToken, backupCode } = await req.json();
  if (!tempToken || !backupCode) {
    return NextResponse.json({ message: 'tempToken e backupCode obrigatórios' }, { status: 400 });
  }

  const key = new TextEncoder().encode(process.env.JWT_SECRET!);
  let payload: { sub: string; type: string; role: string };
  try {
    const result = await jwtVerify(tempToken, key);
    payload = result.payload as typeof payload;
  } catch {
    return NextResponse.json({ message: 'Token temporário inválido' }, { status: 401 });
  }

  if (payload.type !== 'TEMP_2FA') {
    return NextResponse.json({ message: 'Token inválido' }, { status: 401 });
  }

  const isContador = payload.role === 'ACCOUNTANT';
  const usuario = isContador
    ? await prisma.usuarioContador.findUnique({ where: { id: payload.sub }, select: { twoFactorBackupCodes: true } })
    : await prisma.usuarioCliente.findUnique({ where: { id: payload.sub }, select: { twoFactorBackupCodes: true } });

  if (!usuario) return NextResponse.json({ message: 'Usuário não encontrado' }, { status: 404 });

  const idx = await verifyBackupCode(backupCode.toUpperCase(), usuario.twoFactorBackupCodes);
  if (idx === -1) return NextResponse.json({ message: 'Código de backup inválido' }, { status: 401 });

  // Remover código usado
  const newCodes = [...usuario.twoFactorBackupCodes];
  newCodes.splice(idx, 1);

  if (isContador) {
    await prisma.usuarioContador.update({ where: { id: payload.sub }, data: { twoFactorBackupCodes: newCodes } });
  } else {
    await prisma.usuarioCliente.update({ where: { id: payload.sub }, data: { twoFactorBackupCodes: newCodes } });
  }

  const expiresIn = process.env.JWT_EXPIRES_IN ?? '8h';
  const token = await new SignJWT({ sub: payload.sub, role: payload.role })
    .setProtectedHeader({ alg: 'HS256' })
    .setExpirationTime(expiresIn)
    .sign(key);

  return NextResponse.json({ token });
}
```

- [ ] **Step 5: Commit**

```bash
git add app/api/v1/auth/2fa/
git commit -m "feat(2fa): add enable/disable/verify/backup routes"
```

---

## Task 6: Frontend — Step 2FA no login

**Files:**
- Modify: `app/(auth)/page.tsx` (ou onde está o formulário de login)

- [ ] **Step 1: Ler o arquivo atual de login**

Leia `app/(auth)/page.tsx` para entender a estrutura atual.

- [ ] **Step 2: Adicionar state e step de 2FA**

Adicionar um state `step: 'LOGIN' | 'TOTP' | 'BACKUP'` e `tempToken: string`.

Quando o login retornar `{ requires2FA: true, tempToken }`:
- Guardar `tempToken` no state
- Mudar `step` para `'TOTP'`
- Mostrar campo de código TOTP (6 dígitos)
- Link "Usar código de backup" muda para `step = 'BACKUP'`

Ao submeter TOTP: POST `/api/v1/auth/2fa/verify` com `{ tempToken, totpToken }`.
Ao submeter backup: POST `/api/v1/auth/2fa/backup` com `{ tempToken, backupCode }`.

- [ ] **Step 3: Commit**

```bash
git add app/\(auth\)/
git commit -m "feat(2fa): add TOTP verification step to login form"
```

---

## Task 7: Frontend — Seção 2FA nas configurações

**Files:**
- Modify: `app/(contador)/configuracoes/page.tsx`

- [ ] **Step 1: Ler o arquivo atual**

Leia `app/(contador)/configuracoes/page.tsx` para entender a estrutura.

- [ ] **Step 2: Adicionar seção 2FA**

Criar seção "Autenticação em dois fatores" com:
- Se 2FA desativado: botão "Ativar 2FA"
  - Clica → POST `/api/v1/auth/2fa/setup` → exibe QR Code e campo de verificação
  - Input TOTP → POST `/api/v1/auth/2fa/enable` → exibe backup codes para copiar
- Se 2FA ativado: badge "Ativo" + botão "Desativar"
  - Clica → modal pedindo senha → POST `/api/v1/auth/2fa/disable`

- [ ] **Step 3: Commit**

```bash
git add app/\(contador\)/configuracoes/
git commit -m "feat(2fa): add 2FA management section to settings page"
```

---

## Verificação final

- [ ] Testar fluxo completo: login → QR → ativar → logout → login com 2FA → código TOTP
- [ ] Testar uso de backup code
- [ ] Testar desativação com senha
- [ ] Verificar que `twoFactorSecret` nunca aparece em respostas de API (não está no SELECT de nenhuma rota que retorna dados do usuário)
- [ ] `npm run build` passa sem erros
